// CONTROLLERS
const createGame = (request, response) => {
  // @TODO CREATE GAME METHOD
}

const makeMove = (request, response) => {
  // @TODO MAKE MOVE METHOD
}

module.exports = {
  createGame,
  makeMove
}
